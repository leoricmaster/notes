# 数据闭环综合PRD

## 版本历史

| 时间 | 版本 | 作者 | 变更内容 | 状态 |
|------|------|------|----------|------|
| 2023.05.25 | 0.1 | @张云锋 | 初版（总体方案） | 评审稿 |
| 2023.06.28 | 1.0 | @张云锋 | 总体方案评审通过 | 已评审 |
| 2023.07.25 | 0.1 | @张云锋 | 基础数传框架初版 | 评审稿 |
| 2023.08.03 | 0.2 | @张云锋 | 增加产品限制 | 评审稿 |
| 2023.08.04 | 0.1 | @张云锋 | 网络回传初版 | 草稿 |
| 2023.08.14 | 0.2 | @张云锋 | 删除分布式采集需求 | 评审稿 |
| 2023.08.23 | 0.3 | @张云锋 | 细化性能需求 | 评审稿 |
| 2023.09.20 | 0.1 | @张云锋 | 专题问答初版 | 草稿 |
| 2023.10.11 | 0.2 | @张云锋 | 修订PRD | 修订版 |
| 2023.11.08 | 0.1 | @张云锋 | 端侧智能体能力管理初版 | 草稿 |
| 2024.08.14 | 0.2 | @张云锋 | 有灵平台范围调整 | 修订稿 |
| 2024.09.20 | 1.0 | @综合整理 | 综合PRD定稿 | 综合版 |

---

## 目录

1. [业务背景](#一业务背景)
2. [解决方案架构](#二解决方案架构)
3. [产品范围与定位](#三产品范围与定位)
4. [阶段规划](#四阶段规划)
5. [核心功能需求](#五核心功能需求)
6. [端侧智能体能力管理](#六端侧智能体能力管理)
7. [数据压缩与存储](#七数据压缩与存储)
8. [传输服务](#八传输服务)
9. [远程操控](#九远程操控)
10. [运维与监控](#十运维与监控)

---

## 一、业务背景

### 1.1 业务场景

本质需求：大量数据在作业区产生，大量算力在云平台。需要将数据从端侧回传至云侧训练，再将模型推送回端侧，从而实现机器人智能的提升。

从全流程看，本PRD内容直接服务机器人训练、机器人作业用例场景。

#### 1.1.1 初始模型训练

采集行业专家（例如挖机老师傅）的操作数据，回传至云平台训练各类初始模型，使机器人具备一定程度的初始智能。以智能挖机为例，此类模型包括：轨迹控制、行人识别、卡车识别等。

在每个应用方案的早期，都需要经历这个阶段。

#### 1.1.2 模型迭代升级

当经由初始模型推理时，如果置信度低于阈值，或用户反馈推理错误（监控自动作业时），记录为Conner Case。Conner Case的数据需要采集并回传至云平台进行标注、训练和部署，逐步迭代升级模型，从而实现机器人智能的提升，最终实现机器人的自动作业。

**业务流程：**

1. 传感器驱动程序经由ROS写入感知数据，每个传感器对应于不同的主题（topic）
2. 应用程序从不同主题的队列中取出数据，本地实时处理
3. 业务层提供Conner Case检测程序，判断是否有必要采集数据
4. 业务层负责将数据量从采集频率降低到回传频率（例如端侧本地消费时传感器采集频率为30Hz，回传频率可降至1Hz甚至更低）
5. 端侧应用程序通过低代码平台生成的端侧SDK来请求模型
6. 平台层提供数据传输服务，负责将能力请求及配套数据从端侧传输至云侧
7. 云侧进行必要的能力准备（众包标注、训练、评估、压缩、推送、部署）

#### 1.1.3 机器人操控

机器人操控有两类业务场景：

- **训练阶段**：业务专家通过远程操控方式驾驶挖机，获取比传统驾驶方式更为完整的示教数据用于训练
- **作业阶段**：业务专家可以通过远程方式操控机器人，使驾驶员不必身处挖机现场的恶劣危险环境；降低对传统用工的门槛需求

### 1.2 当前挑战

#### 1.2.1 感知数据量大

传感器采集的原始数据量极大。以智能挖机为例：

| 传感器配置 | 数量 | 吞吐量 | 计算说明 |
|-----------|------|--------|----------|
| 主相机 | 1 | 1265.625 Mbps | 1620×960 RGB8 30fps |
| 双目相机 | 2 | 843.75 Mbps | 1280×720 RGB8 20fps |
| 激光雷达 | 1 | 60 Mbps | ~1.5MB/帧，5Hz |
| **总计** | - | **>2Gbps** | - |

#### 1.2.2 基础设施落后

业务现场普遍缺乏网络基础设施。例如桐庐挖机现场没有5G信号，4G信号也极弱，实测在-108 ~ -102 dBm，基本处于断网边缘。

#### 1.2.3 场地改造受限

由于作业环境的特殊性，难以对场地进行网络基础设施改造：
- 缺乏专业人员：现场人员对场地基础设施情况并不了解
- 缺乏改造资金：基础设施改造涉及较大预算，申请周期长

#### 1.2.4 缺乏回传框架

当前缺乏统一的数据回传方案框架，各个项目的零散工作难以沉淀成为平台能力。

#### 1.2.5 需求苛刻多元

| 场景 | 核心关注点 | 特点 |
|------|-----------|------|
| 机器人操控 | 音视频流的实时回传 | 实时性要求极高 |
| 模型训练 | 大量文件的可靠传输 | 可靠性要求高 |

#### 1.2.6 缺乏统一架构

各业务场景在落地时容易各自为战，存在以下隐患：
- 操控端架构日趋庞大，已需要依赖RTX 3090卡
- 即使在局域网内，依然采用三方RTC方式进行数据传输，整系统数据流无法整合

---

## 二、解决方案架构

### 2.1 统一方案架构

#### 2.1.1 基础设施架构

```
┌─────────────────────────────────────────────────────────────┐
│                      中心云平台                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │   训练平台   │  │   模型库    │  │     众包平台         │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
                    ┌─────────┴─────────┐
                    ▼                   ▼
          ┌─────────────────┐  ┌─────────────────┐
          │   广域网/互联网  │  │   5G/专网       │
          └─────────────────┘  └─────────────────┘
                    │                   │
        ┌───────────┴───────────┐      │
        ▼                       ▼      ▼
┌─────────────────┐    ┌─────────────────────────┐
│  边缘计算平台    │◄───│      现场边缘平台        │
│  (MEC/数据中心)  │    │  (工业WiFi/临时组网)    │
└─────────────────┘    └─────────────────────────┘
                                │
                    ┌───────────┴───────────┐
                    ▼                       ▼
          ┌─────────────────┐    ┌─────────────────┐
          │   设备端计算平台  │    │   操控端计算平台  │
          │   (Jetson等)    │    │   (浏览器/RTC)  │
          └─────────────────┘    └─────────────────┘
```

**名词定义：**
- **设备端**：特指与AOP Service不在同一网络域的计算平台，例如工程机械的Jetson基础设施
- **操控端**：用于远程操控机器人的客户端
- **边缘平台**：现场边缘计算平台，可部署在企业侧或租用运营商

#### 2.1.2 数据回传方案矩阵

| 方案 | 适用场景 | 前提条件 | 优势 | 缺点 |
|------|----------|----------|------|------|
| **端侧硬盘摆渡** | 数据量极大/无网络 | 人员配合更换硬盘 | 无网络要求，带宽大 | 人工操作多，实时性低 |
| **边侧硬盘摆渡** | 端侧存储受限 | 局域网基础设施 | 存储时长更长 | 人工操作多，实时性低 |
| **网络实时回传** | 有网络/数据量可控 | 网络基础设施 | 实时性好 | 带宽受限 |

### 2.2 端侧硬盘摆渡方案

**流程：**
1. **数据压缩**：端侧平台对采集数据进行压缩
2. **硬盘更换**：现场人员定期更换硬盘
3. **硬盘快递**：现场人员将硬盘快递至指定云机房接入点
4. **数据导入**：云机房接入点人员导入数据至云侧平台
5. **数据解压**：云侧平台将数据解压缩

**方案需求：**
- 硬件设计支持快速更换硬盘，需支持防震防尘
- 端侧平台支持数据压缩
- 云侧平台支持数据解压缩和数据导入
- 建设云机房接入点，支持高速数据导入（>200Mbps）

### 2.3 边侧硬盘摆渡方案

**流程：**
1. **数据压缩**：端侧平台对采集数据进行压缩
2. **数据传输**：端侧平台通过数据传输服务，将训练数据实时回传至边侧平台
3. **硬盘更换**：现场人员定期更换硬盘
4. **硬盘快递**：现场人员将硬盘快递至指定云机房接入点
5. **数据导入**：云机房接入点人员导入数据至云侧平台
6. **数据解压**：云侧平台将数据解压缩

### 2.4 网络实时回传方案

**流程：**
1. **数据压缩**：端侧平台对采集数据进行压缩
2. **数据回传**：平台提供数据传输服务，将训练数据从端侧平台实时回传至云侧平台
3. **数据解压**：云侧平台将数据解压缩

### 2.5 远程操控数传方案

**三种操控模式对比：**

| 序号 | 操控模式 | 优势 | 缺点 |
|------|----------|------|------|
| 1 | 直连操控 | 理解简单 | 对操控端硬件要求极高（RTX 3090），边际成本极高 |
| 2 | 边缘操控 | 对操控端硬件要求低，数据不出局，回传成本低，操控时延低 | 需要伏羲边侧计算平台 |
| 3 | 云操控 | 对操控端硬件要求低，边际成本低 | 数据会跨域，回传成本高，操控时延大 |

**实现优先级：** 边缘操控 > 直连操控 > 云操控

---

## 三、产品范围与定位

### 3.1 当前问题与调整

#### 3.1.1 平台产品范围蔓延

当前平台承担着与逻辑架构不匹配的职责：
- 平台负责整机应用程序的部署，但无法承担驱动层和业务层软件的运维责任
- 端侧镜像托管带来安全隐患（客户核心资产）
- 介入用户研发流程过多（私服/主干等概念）

#### 3.1.2 范围调整方案

**收缩端侧部署：** 从应用管理收缩至能力管理
- 即从管理用户完整的端侧应用，收缩至管理用户在端侧的模型更新（智能体生命周期管理）
- 包括数据闭环和远程操控

**去除私服主干：** 去除"默认服务器"、"私服"、"主干"、"用户分支"等对用户研发流程的介入

### 3.2 调整后架构

```
┌──────────────────────────────────────────────────────────────┐
│                      用户应用程序                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐   │
│  │  传感器驱动  │  │  业务状态机  │  │   液压臂控制等       │   │
│  └─────────────┘  └─────────────┘  └─────────────────────┘   │
│                                                              │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │              AOP SDK / Agent Lib（有灵平台提供）          │ │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │ │
│  │  │  智能体管理  │  │  数据采集   │  │  模型更新   │     │ │
│  │  └─────────────┘  └─────────────┘  └─────────────┘     │ │
│  └─────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│                      AOP Service（有灵平台）                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐   │
│  │  智能体服务  │  │  数据集管理  │  │   众包平台          │   │
│  └─────────────┘  └─────────────┘  └─────────────────────┘   │
└──────────────────────────────────────────────────────────────┘
```

### 3.3 角色定义

- **智能体开发者**：通过平台开发智能体并托管到平台。聚焦在如何实现智能体的各种能力
- **智能体使用者**：通过平台使用已经托管在平台上的智能体实例，基于智能体开发并完成具体的任务

---

## 四、阶段规划

数据闭环（Epic）方案较为庞大，分阶段实现：

| 阶段 | 特性 | 内容 | 状态 |
|------|------|------|------|
| 阶段一 | Feature#1 | **基础数传框架**：图像数据压缩、采集文件存储、采集文件导入，支持端侧数据摆渡方案 | 已完成 |
| 阶段二 | Feature#2 | **网络回传框架**：基于可靠网络的端云数传框架，支持网络回传方案 | 已完成 |
| 阶段三 | Feature#3 | **端边数传框架**：完成边侧计算平台选型，支持边侧数据摆渡方案 | 规划中 |
| 阶段四 | Feature#4 | **端边云协同**：增强网络回传方案的可靠性，支持网络小时级中断 | 规划中 |
| 阶段五 | Feature#5 | **带宽优化**：整体数传性能提升50% | 规划中 |
| 阶段六 | Feature#6 | **训练自动化**：数据回传至云端后自动触发标注和训练，支持采传标训一体 | 规划中 |
| 阶段七 | Feature#7 | **模型热更新**：模型训练完成后支持不停机热更，支持采传标训做全闭环 | 规划中 |

---

## 五、核心功能需求

### 5.1 统一数传框架

使用统一的数传方案框架支撑数据摆渡方案、网络回传方案两大类方案。

**机制示意：**

```
应用调用 → 数据压缩 → 文件存储 → 传输服务 → 接收端处理 → 平台消费 → 应用消费
```

**传输服务回调机制：**

发送端：
- **就绪回调**：数据通道可用时，从发送队列出队并发送
- **成功回调**：发送成功后，通过文件索引删除文件
- **失败回调**：发送失败后，压入更高优先级队列插队发送

接收端：
- **接收回调**：按需进行功能配置（逻辑编排）
- **反馈**：按通道SLA配置（默认有反馈）
- **去重**：按通道SLA配置（默认有去重）
- **重排序**：按通道SLA配置（默认不重排序）
- **解压**：按业务需要配置（默认不解压）
- **存储**：按业务需要配置（边侧建议有存储）

### 5.2 数据采集接口

#### 5.2.1 能力调用接口

```python
# 标准能力调用
output = agent.capability_name(input)

# 带置信度获取（认知型能力）
result = agent.truck_recognition_ai(main_camera)
if result.confidence < THRESHOLD:
    # 处理Corner Case
    pass
```

#### 5.2.2 数据采集接口（cap.persist）

```python
# 基础用法 - 采集输入和输出
agent.capability_name.persist(input=(param1, param2), output=(result,))

# 只采集输入（适用于初始模型训练阶段）
agent.capability_name.persist(input=(param1, param2), output=None)

# 带标签的采集
agent.capability_name.persist(
    input=(param1, param2),
    output=(result,),
    tag=["winner", "warrior"]
)
```

#### 5.2.3 采集配置接口（cap.persist_config）

```python
# 配置采样率（每N次调用采集一次）
agent.capability_name.persist_config(sample_rate=60)

# 配置条件采集（filter函数返回True时触发采集）
agent.capability_name.persist_config(filter=filter_func)

# 配置去叠帧机制
agent.capability_name.persist_config(
    input=("current_state[-3:-1]", "current_action[-3:-1]"),
    output=("next_state[-3:-1]",)
)
```

### 5.3 数据类型支持

| 数据类型 | 压缩方式 | 压缩比要求 | 硬件加速 |
|----------|----------|-----------|----------|
| 图像 | JPEG | 10倍 | 可选 |
| 视频 | H.264/H.265 | 100倍 | 优先使用（Jetson Orin自带编解码器） |
| 点云 | 无损压缩 | 2.5倍 | - |

**IDL定义示例：**

```python
from typing_extensions import Annotated
from fuxi.aop.edsl import *

class Intelligent_Excavator(Task):
    class Intelligent_Excavator_Agent(Agent):
        # JPEG压缩图像
        @cognition(
            category="AI",
            persistent=False
        )
        def truck_recognition_ai(
            self,
            main_camera: Annotated[Image, JPEG]
        ) -> List[BoundingBox]:
            pass

        # 点云数据（不压缩）
        @cognition(
            category="AI",
            persistent=True
        )
        def truck_pose_estimation(
            self,
            points: List[Points],
            lidar: PointCloud
        ) -> Pose6D:
            pass
```

### 5.4 传输优先级管理

**需求：** 区分数据回传与远程调用的优先级，以免数据回传影响远程调用。

**实现方式：**

```python
# 方式1：专有接口（数据回传）
agent.cap.persist(input, output)  # 低优先级

# 方式2：复合接口（远程调用+数据回传）
output = agent.cap(input)  # 高优先级（调用部分），低优先级（持久化部分）
```

**优先级策略：**
- 远程调用（cap）：高优先级，保证实时性
- 数据回传（cap.persist）：低优先级，可延迟、可丢包重传

### 5.5 网络回传规格

| 规格项 | 要求 |
|--------|------|
| 单端回传带宽 | 1Mbps |
| 支持客户端数 | 4000 |
| 总带宽 | 500 Mbps |
| 带宽调整精度 | 0.1Mbps |
| API响应时间 | < 2ms（非阻塞） |
| 最大缓存 | 可配置（默认100MB） |

---

## 六、端侧智能体能力管理

### 6.1 定义端侧智能体能力

通过IDL中Agent级别的`location = Location.DEVICE`字段定义端侧智能体：

```python
class AutoLoadingTask(Task):
    class AutoLoadingAgent(Agent):
        # 指定智能体的运行位置：端侧
        location: Location = Location.DEVICE

        @cognition(category="AI")
        def truck_recognition(self):
            pass
```

**约束：** 端侧智能体无法定义众包能力（human类型）。如需触发众包任务，需通过云侧智能体中转。

### 6.2 就绪端侧智能体能力

**流程：**
1. 创建智能体：实例化能力，此时模型参数可能为空
2. 拉取模型：从云侧模型库拉取已发布的模型文件
3. 更新能力：用拉取的模型文件更新对应的能力

**API设计：**

```python
# 1. 创建智能体
agent = await sdk.AutoLoadingAgent.create()

# 2. 拉取模型（带回调）
await agent.truck_recognition.fetch(
    tag='latest',  # 或指定版本号
    callback=fetch_callback
)

# 3. 更新能力
def fetch_callback(result: ModelFetchResult):
    if result in [SUCCESS_FROM_REMOTE, SUCCESS_FROM_LOCAL]:
        await agent.truck_recognition.update(tag='latest')
```

**ModelFetchResult枚举：**
- `SUCCESS_FROM_REMOTE`：指定tag的模型文件在云侧匹配成功并拉取成功
- `SUCCESS_FROM_LOCAL`：指定tag的模型文件已存在于本地缓存
- `FAILED_NOT_FOUND_FROM_REMOTE`：指定tag的模型文件在云侧匹配失败
- `FAILED_CONNECTION_TIME_OUT`：网络原因导致拉取失败

### 6.3 使用端侧智能体能力

```python
# 调用能力
bb_list = await agent.truck_recognition(main_camera)

# 采集Corner Case数据
if bb_list.confidence < THRESHOLD:
    agent.truck_recognition.persist(
        input=(main_camera,),
        output=(bb_list,)
    )
```

### 6.4 更新端侧智能体能力

**Subscribe-Notification机制：**

```python
# 订阅模型发布事件
agent.truck_recognition.subscribe(
    event=CapEvent.MODEL_PUBLISH,
    callback=event_callback
)

# 事件回调
def event_callback(event: CapEvent):
    if event == CapEvent.MODEL_PUBLISH:
        # 触发模型拉取
        await agent.truck_recognition.fetch(callback=fetch_callback)
```

**能力闭环流程：**

```
创建能力 → 拉取模型 → 更新能力 → 调用能力 → 采集数据 → 回传数据 → 训练模型 → 发布模型 → (回到拉取模型)
```

### 6.5 模型格式支持

| 源格式 | 目标格式 | 优先级 |
|--------|----------|--------|
| ONNX | MNN | 高 |
| TorchScript | MNN | 中 |
| PyTorch | MNN（通过ONNX中转） | 中 |

---

## 七、数据压缩与存储

### 7.1 功能需求

#### 7.1.1 数据压缩

- 平台需对应用调用进行解析，分解成不同类型的数据（图像、视频、点云）
- 端侧应用通过平台API进行能力请求
- 每次调用的最大采样时间将直接影响存储效率和传输效率（建议不超过30s）
- 避免大文件存储与传输，将大文件分割成小文件（约100MB）
- 调用方需保证同一次调用中多模态数据的对齐

#### 7.1.2 图像压缩

- 支持JPEG压缩和解压缩
- 平均压缩比需达到10倍
- 优先使用硬件加速器
- 如输入已压缩，抛异常

#### 7.1.3 视频压缩

- 支持H.264和H.265压缩和解压缩
- 平均压缩比需达到100倍
- 支持使用硬件加速（可配置：使用/不使用/优先使用-默认）
- 支持时间戳信息随流

#### 7.1.4 点云压缩

- 支持点云集数据压缩和解压缩
- 平均压缩比需达到2.5倍
- 参考：https://pointclouds.org/documentation/structpcl_1_1_p_c_l_point_cloud2.html

### 7.2 文件存储

#### 7.2.1 功能需求

- 支持对压缩后的数据进行持久化，每次应用调用对应一个压缩文件
- 支持在发送文件已收到后，才能删除本地文件
- 支持数据导入接口，以便通过硬盘摆渡的方式快速导入数据
- 硬盘摆渡可认为是一种带宽极大、时延极高的可靠传输服务

#### 7.2.2 文件格式

```
truck_recognition_human1690856655.tar
├── truck_recognition_human1690856655.txt  (元文件)
│   ├── timestamp: 1690856655
│   ├── project id: 0x11111111
│   ├── agent id: 0x22222222
│   ├── sdk version: 1.2.3
│   ├── capability id: 0x33333333
│   └── main_camera: main_camera.jpeg
└── main_camera.jpeg  (压缩后的参数文件)
```

#### 7.2.3 性能需求

- 云机房接入点需支持200Mbps以上的导入带宽
- 以200Mbps导入2TB数据约需22小时

### 7.3 数据完整性验证

**验证指标：**
- 采集数量 P
- 发送数量 S
- 接收数量 R
- 入库数量 O

**正常情况：**
- R = S（接收与发送数目一致）
- O = P（入库与采集数目一致）

**异常情况：**
- O <> P：数据闭环机制出现数据丢失
- R <> S：网络传输服务不可靠

---

## 八、传输服务

### 8.1 功能需求

#### 8.1.1 传输通道

- 为传输通道的建立提供双向认证机制（建议通过证书认证）
- 提供数据传输通道，通道SLA：不丢包，不重复
- 回传任务特点：没有实时性，不要求严格顺序
- 文件传输服务需支持加密传输

#### 8.1.2 传输优化

- 支持插队传输（重传时保证大致顺序）
- 支持断点续传能力
- 支持多进程传输
- 提供通道就绪回调、数据送达回调、发送失败回调、接收成功回调

#### 8.1.3 性能需求

- 提供传输优化能力，可以达到50Mbps的回传传输带宽
- 建议方案：
  - 尝试使用BBR流控替换TCP的传统流控
  - 尝试使用QUIC来承载传输服务，避免使用传统TCP

### 8.2 运维需求

- 统计发送吞吐量信息（分钟、小时、天、周、月）
- 统计接收吞吐量信息
- 展示节点的磁盘剩余可用时间预估：
  ```
  磁盘剩余可用时间 = 分配给数据存储的磁盘剩余空间 / (每小时接收总吞吐量 - 每小时发送总吞吐量)
  ```
- 默认刷新频率30秒

---

## 九、远程操控

### 9.1 功能需求

- 云平台为每个租户统一维护管理网络（设备端、操控端、边缘平台、云平台维持管理通道）
- 支持协调设备端、边缘平台、操控端之间建立有服务等级保障的数据通道
- 支持协调设备端、操控端之间建立有服务等级保障的控制通道
- 边缘平台支持在云平台失联的情况下，协调建立数据通道和控制通道
- 边缘平台支持推理能力和渲染能力

### 9.2 运维需求

- 实时维护管理网络拓扑，并在管理网中断时发出告警
- 实时维护数据网络拓扑，并在数据网中断时发出告警
- 实时维护控制网络拓扑，并在控制网中断时发出告警
- 端侧应用支持在控制网中断时进行紧急停机

---

## 十、运维与监控

### 10.1 数据压缩监控

- 统计采集数据量信息：调用次数、图像数据量、视频数据量、点云数据量、总数据量
- 统计采集吞吐量信息
- 统计每次调用的压缩性能信息（图像/视频/点云压缩比）
- 默认刷新频率30秒

### 10.2 传输服务监控

- 统计发送/接收吞吐量
- 统计丢包率、重传率
- 磁盘剩余可用时间预估

### 10.3 告警机制

| 告警类型 | 触发条件 | 级别 |
|----------|----------|------|
| 管理网中断 | 无法连接到管理通道 | 严重 |
| 数据网中断 | 无法传输数据 | 警告 |
| 控制网中断 | 无法控制设备 | 严重 |
| 压缩比异常 | 长期低于阈值 | 警告 |
| 磁盘空间不足 | 剩余可用时间低于阈值 | 警告 |

---

## 附录A：用户旅程示例

### A.1 智能挖机数据摆渡方案

```python
#!/usr/bin/env python3
from fuxi.aop.core import AOP
import sdk

async def async_main():
    aop = await AOP.init(task_type=sdk.Intelligent_Excavator)
    agent = await aop.create_agent(sdk.Intelligent_Excavator_Agent)

    # 卡车识别相关逻辑
    result = agent.truck_recognition_ai(main_camera)
    if result.confidence < 0.8:  # Corner Case检测
        agent.truck_recognition_human(main_camera)  # 触发数据采集

if __name__ == "__main__":
    asyncio.get_event_loop().run_until_complete(async_main())
```

### A.2 网络回传方案（游戏场景）

```python
#!/usr/bin/env python3
from fuxi.aop.core import AOP
import sdk

async def async_main():
    aop = await AOP.init(task_type=sdk.BattleAI)
    agent = await aop.create_agent(sdk.BattleAI_Agent)

    # 采集真实数据用于训练
    agent.move_policy_ai.persist(
        input=(combo_id, vector, important),
        output=(move, dodge, attack, other),
        bw="0.5Mbps",  # 限制带宽
        cache="50MB"   # 设置缓存
    )

if __name__ == "__main__":
    asyncio.get_event_loop().run_until_complete(async_main())
```

### A.3 端侧能力更新

```python
#!/usr/bin/env python3
import sdk

async def main():
    # 1. 创建智能体
    agent = await sdk.AutoLoadingAgent.create()

    # 2. 订阅模型发布事件
    agent.truck_recognition.subscribe(
        event=CapEvent.MODEL_PUBLISH,
        callback=on_model_publish
    )

    # 3. 初始拉取模型
    await agent.truck_recognition.fetch(callback=on_fetch_complete)

    # 4. 业务逻辑...
    while True:
        result = await agent.truck_recognition(main_camera)
        if result.confidence < 0.8:
            agent.truck_recognition.persist(input=(main_camera,), output=(result,))

def on_model_publish(event):
    """模型发布事件回调"""
    if event == CapEvent.MODEL_PUBLISH:
        print("New model published, fetching...")
        # 触发模型拉取

def on_fetch_complete(result):
    """模型拉取完成回调"""
    if result in [SUCCESS_FROM_REMOTE, SUCCESS_FROM_LOCAL]:
        await agent.truck_recognition.update()
        print("Model updated successfully")

if __name__ == "__main__":
    asyncio.run(main())
```

---

## 附录B：术语表

| 术语 | 英文 | 定义 |
|------|------|------|
| AOP框架 | AOP Framework | 用户通过pip命令安装的AOP开发包 |
| 任务开发SDK | Task Develop SDK | 用户通过IDL文件生成的带有任务属性的SDK |
| 智能体库 | Agent Lib | 基于IDL文件生成的智能体库 |
| 模型 | Model | 通过各种机器学习平台训练好的模型文件 |
| 能力 | Capability | 模型文件服务化之后可被调用的服务 |
| Corner Case | Corner Case | 模型推理置信度低于阈值或用户反馈错误的场景 |
| 数据摆渡 | Data Ferry | 通过物理介质（硬盘）传输数据的方式 |
| 数据回传 | Data Upload | 通过网络传输数据的方式 |

---

## 附录C：参考文档

1. PRD-端侧数据回传
2. PRD-数据闭环-总体方案
3. PRD-数据闭环-基础数传框架
4. PRD-数据闭环-网络回传
5. PRD-数据闭环-专题问答
6. PRD-数据闭环-端侧智能体能力管理
7. PRD-数据闭环-有灵平台范围调整
